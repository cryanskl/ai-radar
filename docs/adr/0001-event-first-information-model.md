# Use an event-first information model

AI Radar represents one real-world change as an `Event` supported by one or more `Source Items`, rather than publishing every source article as an independent primary record. This costs more in clustering and editorial review than an article feed, but prevents duplicate coverage from becoming the product, keeps original provenance intact, and lets news accumulate into entity histories and relationships.
